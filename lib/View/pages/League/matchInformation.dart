import 'package:flutter/material.dart';

class MatchInforation extends StatelessWidget {
  const MatchInforation({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
